Runs a local test server on port 4567.

Requirements
 * sinatra    - sudo gem install sinatra
 * sequel     - sudo gem install sequel
 * faker      - sudo gem install faker
 * json       - sudo gem install json
 * xml-simple - sudo gem install xml-simple